import React, { useState } from 'react';

import { useTodoLayerValue } from '../context/todoContext';

function Todo({ todo }) {
   return;
   <div>
      <div>{todo.input}</div>
   </div>;
}

export default Todo;
